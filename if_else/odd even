#include <stdio.h>
//even odd

int main()
{
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    int r = num % 2; // remainder

    if (r == 0)
    {
        printf("The given number is even.\n");
    }
    else
    {
        printf("The given number is odd.\n");
    }
}
